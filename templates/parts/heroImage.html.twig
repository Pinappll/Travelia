<br>
<section class="relative">
    <!-- Image de fond -->
    <img
            src="{{ asset('images/hero-image.jpg') }}"
            alt="Hero Image"
            class="w-full  h-[400px] object-cover rounded-lg shadow-lg"
    >

    <!-- Titre superposé -->
    <div class="absolute inset-0 flex items-center justify-center">
        <h1 class="text-white text-4xl md:text-6xl font-bold bg-black/50 px-6 py-3 rounded-lg shadow-md">
            Bienvenue chez Travelia
        </h1>
    </div>
</section>
